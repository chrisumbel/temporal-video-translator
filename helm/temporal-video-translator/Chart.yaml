apiVersion: v2
name: temporal-video-translator
description: A Helm chart for the video-translator Temporal worker plus a self-contained Temporal server (auto-setup + postgres + UI)
type: application
version: 0.1.0
appVersion: "0.1.0"
